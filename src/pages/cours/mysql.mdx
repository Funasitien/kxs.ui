---
title: "Fiche synthétique des commandes MySQL"
icon: "mdi:paper-remove"
layout: "/src/layouts/Page.astro"
---

### Afficher les bases de données

```sql
SHOW DATABASES;
```

### Créer une base de données

```sql
CREATE DATABASE base_de_donnees;
```

### Utiliser une base de donnée

```sql
USE base_de_donnees;
```

### Supprimer une base de données

```sql
DROP DATABASE base_de_donnees;
```

À ne pas utiliser à la légère… des personnes ont perdu leur emploi (ou
beaucoup d'argent) à cause de cette commande !

### Importer une base de données à partir d'un fichier

Il faut d'abord créer la base et l'utiliser.

```sql
SOURCE fichier.sql;
```

## Tables

Ces commandes sont valables lorsqu'on a déjà sélectionné une base de données
(avec USE).

### Afficher les tables

```sql
SHOW TABLES;
```

### Afficher la structure d'une table

```sql
DESCRIBE table;
```

### Afficher le contenu d'une table

```sql
SELECT * FROM table;
```

### Afficher certains champs d'une table avec des conditions

```sql
SELECT champ1, champ2… FROM table WHERE conditions;
```

### Modification des lignes d'une table

```sql
UPDATE table
SET champ1 = 'nouvelle_valeur',
    champ2 = 'autre_nouvelle_valeur',
    …
WHERE condition
```

### Insertion d'un ligne complête dans une table

```sql
INSERT INTO table
VALUES ('valeur champ 1', 'valeur champ 2', …);
```

### Insertion d'un ligne en ne saisissant que certains champs dans une table

```sql
INSERT INTO table (champ1, champ2, …)
VALUES ('valeur champ 1', 'valeur champ 2', …);
```

### Supprimer des lignes d'une table

```sql
DELETE FROM table
WHERE condition;
```

### Afficher les champs d'une table par ordre croissant de _champ_

```sql
SELECT * FROM table ORDER BY champ ASC;
```

### Afficher les champs d'une table par ordre décroissant de _champ_

```sql
SELECT * FROM table ORDER BY champ DESC;
```

### Effectuer une jointure avec _champ_ entre deux table

```sql
SELECT * FROM table1 JOIN table2 USING(champ);
```

### Ajouter une colonne à une table

```sql
ALTER TABLE table
ADD champ type;
```

### Créer une nouvelle table

```sql
CREATE TABLE table
(
    champ1 type1,
    champ2 type2,
    …
);
```